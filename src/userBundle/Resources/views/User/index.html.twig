{% extends "userBundle:User:layout.html.twig" %}

{% block title %}
  Accueil - {{ parent() }}
{% endblock %}


{% block main %}
<h2>contenu site User</h2>
<div class="row">

  <!-- profil de l'utilisateur -->
<div class="row col s3">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          <span class="card-title">{{ app.user.username }}</span>
          <p>{{ app.user.username }}</p>
          <p>{{ app.user.email }}</p>
          <p>{{ app.user.username }}</p>
          <p>{{ app.user.email }}</p>
          <p>{{ app.user.username }}</p>
          <p>{{ app.user.email }}</p>
          <p>{{ app.user.username }}</p>
          <p>{{ app.user.email }}</p>
          <p>{{ app.user.username }}</p>
          <p>{{ app.user.email }}</p>
          <p>{{ app.user.username }}</p>
          <p>{{ app.user.email }}</p>
          <p>{{ app.user.username }}</p>
          <p>{{ app.user.email }}</p>
        </div>
      </div>

      <div class="col s12">
          <div class="card blue-grey darken-1">
            <div class="card-content white-text">
              <span class="card-title">{{ app.user.username }}</span>
              <p>{{ app.user.username }}</p>
              <p>{{ app.user.email }}</p>
              <p>{{ app.user.username }}</p>
              <p>{{ app.user.email }}</p>
              <p>{{ app.user.username }}</p>
              <p>{{ app.user.email }}</p>
              <p>{{ app.user.username }}</p>
              <p>{{ app.user.email }}</p>
              <p>{{ app.user.username }}</p>
              <p>{{ app.user.email }}</p>
              <p>{{ app.user.username }}</p>
              <p>{{ app.user.email }}</p>
              <p>{{ app.user.username }}</p>
              <p>{{ app.user.email }}</p>
            </div>
          </div>
        </div>
        
  </div>

  <!-- ajouter un article -->
  <div class="rox col s6">

  <div class="row col s12">
        <div class="card blue-grey darken-4">
          <div class="card-content white-text">
            <span class="card-title">ajouter un article</span>
          </div>
          <div class="card-action">
              <button>envoyer</button>
          </div>
        </div>
      </div>

      <!-- article publier -->
      
          <div class="row col s12">
            <div class="card">
              <div class="card-image">
                <img src="{{ asset('cartable.jpg') }}">
                <span class="card-title">Card Title</span>
                <a class="btn-floating halfway-fab waves-effect waves-light red"><i class="material-icons">add</i></a>
              </div>
              <div class="card-content">
                <p>I am a very simple card. I am good at containing small bits of information. I am convenient because I require little markup to use effectively.</p>
              </div>
            </div>
          </div>
  </div>
      

   <!-- autre  -->

   <div class="row col s3">
      <div class="col s12">
        <div class="card blue-grey darken-1">
          <div class="card-content white-text">
            <span class="card-title">{{ app.user.username }}</span>
            <p>{{ app.user.username }}</p>
            <p>{{ app.user.email }}</p>
            <p>{{ app.user.username }}</p>
            <p>{{ app.user.email }}</p>
            <p>{{ app.user.username }}</p>
            <p>{{ app.user.email }}</p>
            <p>{{ app.user.username }}</p>
            <p>{{ app.user.email }}</p>
            <p>{{ app.user.username }}</p>
            <p>{{ app.user.email }}</p>
            <p>{{ app.user.username }}</p>
            <p>{{ app.user.email }}</p>
            <p>{{ app.user.username }}</p>
            <p>{{ app.user.email }}</p>
          </div>
        </div>
      </div>


      <div class="col s12">
          <div class="card blue-grey darken-1">
            <div class="card-content white-text">
              <span class="card-title">{{ app.user.username }}</span>
              <p>{{ app.user.username }}</p>
              <p>{{ app.user.email }}</p>
              <p>{{ app.user.username }}</p>
              <p>{{ app.user.email }}</p>
              <p>{{ app.user.username }}</p>
              <p>{{ app.user.email }}</p>
              <p>{{ app.user.username }}</p>
              <p>{{ app.user.email }}</p>
              <p>{{ app.user.username }}</p>
              <p>{{ app.user.email }}</p>
              <p>{{ app.user.username }}</p>
              <p>{{ app.user.email }}</p>
              <p>{{ app.user.username }}</p>
              <p>{{ app.user.email }}</p>
            </div>
          </div>
        </div>
    </div>


    </div>


</div>
  

{% endblock %}
