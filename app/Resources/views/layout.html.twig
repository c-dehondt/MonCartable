<!DOCTYPE html>

<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

        <title>{% block title %}Mon Cartable{% endblock %}</title>

        {% block stylesheets %}
            {# On charge le CSS de materialize depuis le site directement #}
            {# We load the materialize CSS from the site directly #}
            <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
            <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.100.2/css/materialize.min.css">
            <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        {% endblock %}

    </head>

    <body>
      <header>
        <nav class="nav-wrapper brown darken-3">
          <a href="/" class="brand-logo">Mon Cartable</a>
          <p>
          </p>
          <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="material-icons">menu</i></a>
          <ul class="right hide-on-med-and-down">
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
    
            <li><a href="{{ path('profil') }}"><i class="material-icons">person</i></a> </li>  
            <li><a class="nav-item nav-link active" href="{{ path('fos_user_security_logout') }}">Déconnexion</a>
              {% else %}
              <li><a class="nav-item nav-link active" href="{{ path('user') }}">Connexion</a></li
              {% endif %}
           
            
          </ul>
          <ul class="side-nav" id="mobile-demo">
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <li>     
                  <a class="nav-item nav-link active" href="{{ path('fos_user_security_logout') }}">Déconnexion</a>
                  <a href="{{ path('profil') }}"><i class="material-icons">person</i></a>
                  {% else %}
                  <a class="nav-item nav-link active" href="{{ path('user') }}">Connexion</a>
                  {% endif %}
                </li>
                
          </ul>
      </nav>
     </header>

                    
            <main class="container row">
                
                <div id="content" class="col-12">
                {% block main %}
                <div>
                    <img class="responsive-img" src="{{ asset('cartable.jpg') }}" id="test" alt="page d'accueil">  
                </div>
                {% endblock %}
                </div>

            </main>


    <hr>

    <footer>
      <p>The sky's the limit © {{ 'now'|date('Y') }} and beyond.</p>
    </footer>




   {% block javascripts %}
    {# Ajoutez ces lignes JavaScript si vous comptez vous servir des fonctionnalités du materialize Twitter #} 
    <script type="text/javascript" src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
    <script type="text/javascript" src="js/materialize.min.js"></script> 
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.100.2/js/materialize.min.js"></script>
    <script src="{{ asset('js/main.js') }}"></script> 
     
    
    {% endblock %}

</body>
</html>